version: 0.1
runson: ${matrix.os}
matrix:
  os: [win, mac]

exclusionMatrix: 
  - os: [win]
    browser: ["safari"]

concurrency: 4

framework:
  name: maven/testng
  
runtime:
  language: java
  version: 11

testSuites:
  - mvn test -Denv="lambdaTest"

jobLabel: ['Testing', 'CrossBrowserSuite']